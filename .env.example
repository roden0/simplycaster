# SimplyCaster Environment Variables

# Database Configuration (Alternative to Docker secrets)
# DATABASE_URL=postgres://user:password@localhost:5432/dbname
# Or use individual components:
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=app
# DB_PASSWORD=your_secure_password
# DB_NAME=appdb

# Application Configuration
PORT=8000
NODE_ENV=development

# Security Configuration (Alternative to Docker secrets)
# JWT_SECRET=your-super-secure-jwt-key-min-32-characters
# PEPPER_SECRET=your-global-pepper-for-password-hashing-min-32-chars
JWT_EXPIRES_IN=7d

# OAuth Configuration (if using external OAuth providers)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# File Storage Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=100MB

# Email Configuration (for invitations)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# FROM_EMAIL=noreply@simplycaster.com

# Redis Configuration (for sessions/caching)
# REDIS_URL=redis://localhost:6379

# Application URLs
APP_URL=http://localhost:8000
FRONTEND_URL=http://localhost:8000

# ============================================================================
# DOCKER SECRETS (Recommended for Production)
# ============================================================================
# When using Docker Compose, secrets are automatically read from:
# - /run/secrets/db_user
# - /run/secrets/db_password  
# - /run/secrets/db_name
# - /run/secrets/jwt_secret
# - /run/secrets/pepper_secret
#
# Generate production secrets with: deno task secrets:generate